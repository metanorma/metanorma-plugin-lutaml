{%- for attr in attributes -%}
  {%- if attr.has_association? == false -%}
    | {{ attr.upper_klass }}:{{ attr.name }}
    | {{ attr.type }} [{{ attr.cardinality }}]   
    | {{ attr.definition }}
  {%- endif -%}
{%- endfor -%}