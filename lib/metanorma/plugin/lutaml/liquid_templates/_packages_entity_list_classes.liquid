.{{ package.name }}（{{ package.stereotype }}）中定义的 {{ type }}
[cols="2a,6a",options="header"]
|===
|Name |Description

{% for klass in classes %}
| <<section-{{ klass.xmi_id }},{{ klass.name }}>> «{{ klass.stereotype | default: "Enumeration" }}»
| {{ klass.definition | replace: '|', '\|' }}

{% endfor %}
|===